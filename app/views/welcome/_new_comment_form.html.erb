<%= form_for [post, post.comments.new] do |f| %>
<div class="submit-comment comment input-group">
  <%= f.text_field :content, class: "form-control" %>
  <span class="input-group-btn">
    <%= f.submit "Comment", class: "btn btn-primary" %>
  </span>
</div>
<% end %>
